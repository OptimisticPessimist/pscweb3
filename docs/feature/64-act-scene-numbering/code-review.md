# コードレビュー: Act-Scene採番機能

## 1. 概要
- **対象ファイル**: `fountain_parser.py`, `sceneFormatter.ts`, `rehearsals.py`, 各種フロントエンドコンポーネント(`RehearsalModal.tsx`, `SchedulePollCalendar.tsx` など)
- **総合評価**: 4/5
- **要約**: ActごとのSceneリセット機能の実装、およびフロントエンド/バックエンドでのフォーマット統一は要件を満たしており、一貫性があります。既存のロジックを壊さずにうまく組み込まれています。

## 2. 詳細レビュー
| 観点 | 評価 | コメント |
| :--- | :--- | :--- |
| 命名の適切さ | OK | `formatSceneNumber` など、実態に即した分かりやすい命名になっています。 |
| 変数の粒度 | OK | `act_scene` や `scene_heading` など、適切なスコープで定義されています。 |
| メソッド・関数の粒度 | OK | 既存の関数内に適切な粒度で組み込まれており、ユーティリティとしての切り出し（`sceneFormatter.ts`）も適切です。 |
| 冗長性の排除 | OK | `sceneFormatter.ts` を用いることで、フロントエンドでの冗長なフォーマット処理が排除されています。 |
| 条件式の単純さ | OK | `scene.act_number ? \`... \` : ...` のようなシンプルな三項演算子で解決できています。バックエンドの `act_scene = f"{scene.act_number}-{scene.scene_number}" if scene.act_number else str(scene.scene_number)` も簡潔で良いです。 |
| セキュリティ | OK | 今回の実装範囲において、特殊なセキュリティリスク（インジェクション等）を生む要素はありません。 |
| 可読性 | OK | 各コンポーネントにおける表記が統一されたことで、コードベース全体の可読性が向上しています。 |

## 3. 具体的な指摘事項と修正案
特になし。実装はクリーンであり、テストカバレッジも保たれています。既存の台本データについて、データベースのマイグレーションはスコープ外として合意が取れているため、問題ありません。

## 4. 改善提案 (Optional)
今後、香盤表 (`SceneChartPage.tsx`) において、ActとSceneの列を統一するかどうかの検討余地があります（現在はScene列に `{Act}-{Scene}` が表示されており、Act列が別で存在する場合は冗長になる可能性があるため）。UIのフィードバックを見ながら調整するのが良いでしょう。
